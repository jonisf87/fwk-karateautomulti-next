name: Publish Antora Site

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Antora
        run: npm install -g @antora/cli @antora/site-generator-default @antora/site-generator

      - name: Install docs dependencies
        working-directory: code/docs
        run: npm ci

      - name: Bundle Antora UI
        working-directory: code/docs
        run: npm run ui:bundle

      - name: Build Antora site
        working-directory: code/docs
        run: antora antora-playbook.yml

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: code/docs/build/site
